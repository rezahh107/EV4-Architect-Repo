# Architect Stage Payload fixtures

All fixtures in this directory are synthetic.

```text
valid/
invalid/
insufficient-evidence/
```

They test `ev4-architect-stage-payload@1.0.0` schema and semantic validator behavior. They must not be described as real Architect exports or real Project Gate handoffs.

The invalid directory may use a case-pack fixture with named mutation cases. The validator expands each case and reports it separately.
