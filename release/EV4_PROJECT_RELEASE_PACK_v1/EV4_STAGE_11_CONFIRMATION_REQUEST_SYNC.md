# Stage 11 confirmation sync

Status: active

Required package field:

```text
confirmation_request
```

Required child fields:

```text
confirmation_id
confirmed_action_ids
expected_user_token
template_id
```

The first builder batch uses action ids such as:

```text
BATCH-001-A01
BATCH-001-A02
BATCH-001-A03
```

The user token is:

```text
تایید BATCH-001
```

Template id:

```text
standard_batch_confirmation
```
